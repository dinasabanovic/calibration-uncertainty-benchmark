# Additional dependencies for the major revision on top of the
# existing requirements.txt. Add these lines to requirements.txt
# (or pip install -r requirements_revision.txt after the main install).

catboost>=1.2          # CatBoostModel (revision_helpers.py)
dirichletcal>=0.4      # DirichletODIRCalibrator (revision_helpers.py)
statsmodels>=0.14      # multipletests for practical-H7 (run_ood_diagnostics.py)

# Optional — only needed for run_ood_diagnostics.py --section tabpfn.
# Requires a TabPFN API token; export TABPFN_TOKEN=<token> before running.
tabpfn>=2.0
